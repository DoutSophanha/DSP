{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n web,api,worker -c cyan,green,magenta \"pnpm --dir frontend dev\" \"PYTHONPATH=. ../.venv/bin/uvicorn app.main:app --app-dir backend --reload --port 8000\" \"../.venv/bin/python workers/job_worker.py\"",
    "dev:web": "pnpm --dir frontend dev",
    "dev:api": "PYTHONPATH=. ../.venv/bin/uvicorn app.main:app --app-dir backend --reload --port 8000",
    "dev:worker": "../.venv/bin/python workers/job_worker.py",
    "build": "pnpm --dir frontend build",
    "start": "pnpm --dir frontend start",
    "lint": "pnpm --dir frontend lint"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
